<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.solvd.Uver.daos.SuperCarDAO">
    <resultMap id="SuperCarResultMap" type="com.solvd.Uver.entities.SuperCar" autoMapping="false">
        <result column="super_car_make" property="make"/>

        <result column="super_car_model" property="model"/>
    </resultMap>

    <select id="getById" parameterType="Integer" resultMap="SuperCarResultMap">
        Select * from superCar where idSuperCar =#{id}
    </select>

    <select id="getAll" resultMap="SuperCarResultMap">

    </select>

    <delete id="delete">

    </delete>
    <insert id="save">

    </insert>
    <update id="update">
        UPDATE superCar SET model = #{model}, make = #{make} WHERE idSuperCar = #{id};
    </update>
</mapper>
